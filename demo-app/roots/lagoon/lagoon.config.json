{
  "about": "The reference ocean's lagoon: the same components the bridge embeds, run against fixtures. The dev proxy is here for `motu fixtures record --transport http`, which captures those fixtures from the real legacy backend.",
  "title": "motu demo-app",
  "proxy": {
    "/api": "https://localhost:8443"
  },
  "stations": {
    "members": {
      "label": "Members",
      "order": 1
    },
    "admin": {
      "label": "Org Lookup",
      "order": 2
    },
    "users": {
      "label": "Users",
      "order": 3
    }
  }
}
