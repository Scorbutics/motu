// Types for ./dock.mjs — the lagoon dock, drawn by whoever hosts the lagoon.
/** The dock's stylesheet, scoped to `#tide`. */
export function motuDockCss(): string
/** The dock bootstrap as source, for a page that cannot import a module. */
export function motuDockJs(): string
