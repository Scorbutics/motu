// What a stylesheet or an image becomes under the node harness. A bundler turns these into a class
// map or a URL; node cannot parse them at all, and refusing to load one would fail an island for
// importing its own CSS — which says nothing about whether the island works.
export default '';
export const __motuAssetStub = true;
